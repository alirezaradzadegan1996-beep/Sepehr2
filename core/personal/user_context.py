
class UserContext:

    def load(self):

        return {
            "user": "owner",
            "memory": "available",
            "preferences": "known",
            "status": "loaded"
        }


user_context = UserContext()
